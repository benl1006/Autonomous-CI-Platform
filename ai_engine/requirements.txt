# Core web framework + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database — async SQLAlchemy + PostgreSQL driver
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0

# Vector similarity search (pgvector Python types + SQLAlchemy integration)
pgvector>=0.3.2
numpy>=1.26.0

# Async task queue — Celery uses Redis as broker and result backend
celery[redis]>=5.4.0
redis>=5.0.8

# Embedding model — all-MiniLM-L6-v2 produces 384-dimensional vectors
sentence-transformers>=3.0.0

# LLM client — Google Gemini for patch generation (Phase 4)
google-genai>=0.1.0

# Settings management — reads env vars / .env files via Pydantic
pydantic-settings>=2.3.0

# Async HTTP client — used to POST AIEngineResponse callbacks to the orchestrator
httpx>=0.27.0

# Test suite — `make test-ai` / `docker-compose run ai-service pytest` need this
# installed in the image, not just on the host.
pytest>=8.0.0
